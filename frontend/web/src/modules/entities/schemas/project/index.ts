/**
 * entities/schemas/project — barrel export for the project domain schemas.
 *
 * Import from this file rather than individual schema files so callers have
 * a single stable import path regardless of how the files are reorganised.
 *
 * @module
 */
export * from "./response";
export * from "./input";
export * from "./actions";
export * from "./forms";
