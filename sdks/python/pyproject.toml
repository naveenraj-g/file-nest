[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "filenest"
version = "0.1.0"
description = "FileNest SDK for Python — sync and async client for file infrastructure"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
    "pydantic>=2.0",
]

[project.optional-dependencies]
django = ["django>=4.2"]
fastapi = ["fastapi>=0.100"]
dev = ["pytest", "pytest-asyncio", "respx", "python-dotenv"]

[project.urls]
homepage = "https://github.com/naveenraj-g/file-nest"
repository = "https://github.com/naveenraj-g/file-nest"
documentation = "https://github.com/naveenraj-g/file-nest"

[tool.hatch.build.targets.wheel]
packages = ["filenest"]
