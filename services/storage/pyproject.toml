[project]
name = "filenest-storage"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "filenest-shared",
    # S3-compatible storage (RustFS, AWS S3, Cloudflare R2, Backblaze B2, DigitalOcean Spaces)
    "aioboto3>=13.2",
    "boto3>=1.35",
]
