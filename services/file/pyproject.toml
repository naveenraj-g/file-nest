[project]
name = "filenest-file"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "filenest-shared",
    "filenest-storage",
    "python-multipart>=0.0.17",
    "aiofiles>=24.1",
    "python-magic>=0.4",
]
